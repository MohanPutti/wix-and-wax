// ─── Feature Flags ───────────────────────────────────────────────────────────
// Toggle these to enable/disable features across the entire app.

/** Set to true to apply 18% GST at checkout. */
export const ENABLE_GST = false

export const GST_RATE = 0.18
